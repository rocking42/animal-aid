$(document).ready(function() {

    var video = document.querySelector("video");
    video.pause();

    // $(window).on("keypress", function(e) {
    //   if (e.originalEvent.code === "Space") {
    //       var isPlaying = $("video");
    //       isPlaying[0].pause();
        //   if (isPlaying[0].paused === true) {
        //       video.pause();
        //   }
    //   }
    // }, function(){
    //     var isPlaying = $("video");
    //       if (isPlaying[0].paused === false) {
    //           isPlaying[0].pause();
    //       }
    // });

});
